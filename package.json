{
  "name": "mazarifi",
  "version": "0.1.0",
  "private": true,
  "description": "Mazari Fi — otimizador/agregador de yield (Fase 1: inteligência read-only de pools)",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
